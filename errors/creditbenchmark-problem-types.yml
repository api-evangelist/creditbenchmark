generated: '2026-07-18'
method: derived
source: openapi/creditbenchmark-openapi-original.yml
format: custom-envelope
envelope:
  shape: '{ "error": { "code": string, "message": string, "details": array|object|string } }'
  required: [code, message]
  notes: >-
    Credit Benchmark uses a custom error envelope (not RFC 9457 problem+json). The
    machine-readable `error.code` is the stable field; `error.message` is human-readable;
    `error.details` carries structured ValidationErrorDetail[] on 422.
  content_type: application/json
problems:
  - status: 400
    code: BAD_REQUEST
    title: Bad Request
    component: '#/components/responses/BadRequest'
    remediation: Check request body against the RequestEnvelope schema for the route.
  - status: 401
    code: UNAUTHORIZED
    title: Unauthorized
    component: '#/components/responses/Unauthorized'
    remediation: 'Obtain a JWT via POST /gartan/api/token and send header Authorization: Bearer <token>.'
  - status: 403
    code: FORBIDDEN
    title: Forbidden
    component: '#/components/responses/Forbidden'
    remediation: The authenticated user lacks entitlement for the requested data/columns.
  - status: 422
    code: VALIDATION_ERROR
    title: Validation Error
    component: '#/components/responses/ValidationError'
    remediation: Inspect error.details[] (ValidationErrorDetail) for the offending fields.
  - status: 500
    code: INTERNAL_SERVER_ERROR
    title: Internal Server Error
    component: '#/components/responses/InternalServerError'
    remediation: Retry later; contact api-support@creditbenchmark.com if persistent.
applies_to_operations:
  - customAggregate
  - entityRatingChange
  - portfolioData
  - portfolioSummary
  - ratingDistribution
  - clientDataCustomAggregate
  - clientDataRatingDistribution
  - clientDataPortfolioSummary
  - clientDataEntityRatingChange
  - matchEntities
